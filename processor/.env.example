# Processor Configuration
# Copy to .env and customize values

# Paths (defaults are for Docker deployment)
CONFIG_PATH=/app/config.yaml
DB_PATH=/app/data/notifications.db
CONTACTS_PATH=/app/contacts.json

# Ollama LLM settings
OLLAMA_URL=http://ollama:11434
OLLAMA_MODEL=qwen2.5:7b

# Pi health check endpoint (for dashboard connection status)
PI_HEALTH_URL=http://192.168.1.236:8081/health
